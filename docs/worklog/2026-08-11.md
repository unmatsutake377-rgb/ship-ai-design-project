# 2026-08-11 — NSGA 3차 진화: "심판 없는 곳으로 도망친다" 3탄 (이번엔 배가 문제)

## 실행 (배경 nohup, 08-10 오후 발사)

- 초대예산 pop 256 × gen 200 = **5.1만 평가** (v2의 4배), RMS
  원전 승급 후 첫 진화. 진화 + 전선 전체 8중 재검을 한 프로세스로
  완주 (outputs/pareto_large_v3_evo.csv·pareto_v3_full_recheck.csv)
- 사망 집계: 제약 12,986 / 게이트 7,294 / 오류 6,284 / 생존 24,636
  (48% — v2 대비 급등, roll 잣대 정상화 효과)

## fast 성적 — 전선 183척 (v2의 1.9배), 단가 40% 급락

| 항목 | v2 | v3 fast |
|---|---|---|
| 전선 | 97척 | 183척 |
| 경제형 단가 | 0.442 $/kt·nm | **0.268 (−39%)** |
| 속도형 단가 | 1.657 | **0.994 (−40%)** |
| Cb 협곡 | 0.49~0.52 | **0.463~0.480 (더 날씬)** |

과엄 roll 잣대가 풀리자 진화가 더 날씬한 대역으로 이동 — 어제
잣대가 막던 설계 공간이 실재했음.

## 전체 재검 — 완전 검증 0 / 183 (전멸)

- 내항: **183/183 전원 합격** (roll RMS 0.27~0.74 vs 6 초여유,
  pitch 3.0~3.3 경고 표기만) — RMS 승급 체계 정상 작동
- **조종: 183/183 전원 불합격 (스킵 아닌 실판정)** — advance
  5.17 > 4.5L·전술직경 6.51 > 5.0L (지그재그는 통과). 날씬한
  배는 침로 유지엔 유리하나 **선회 관성이 커서 IMO 선회 기준
  탈락** — Kijima 슬렌더 외삽 C급 판정 전제
- **"진화는 심판 없는 곳으로 도망친다" 3탄**: v1 roll → v2
  pitch(잣대 오독) → v3 선회. 앞 두 번과 결정적 차이 — **이번엔
  잣대가 아니라 배가 문제** (기준은 IMO 원전 A급, 계수만 C급).
  fast 게이트에 조종이 없어서 진화가 그 사각지대로 40% 단가
  이득을 사러 감

## 판정·다음

- **공인 챔피언 전선 = v2 97척 유지** (완전 검증). v3는 "조종
  제약이 활성 제약(binding constraint)임을 실증한 탐사"로 박제
- 백로그 최우선: **fast에 조종 프록시** — Kijima 선형 계수로
  방향 안정성/선회 지표 닫힌식 (시뮬 없이 수식 몇 개, roll
  프록시의 조종판) → v4 재진화. 차선: Jensen 닫힌식 heave/pitch
- 교훈: 게이트를 fast에 하나 넣을 때마다 진화는 다음 빈
  심판석을 찾아냄 — fast·full 정합은 전 게이트 프록시가 목표

---

# 조종 프록시 회차 — fast의 마지막 사각지대 봉쇄 (32일차분)

## 발상 — 수식 암기 대신 같은 모델을 다르게 풀기

- 후보였던 Kijima 선형 계수 닫힌식(원전 재대조 필요) 대신
  **정상 선회 닫힌 해법** 채택: 전체 게이트가 쓰는 **같은 MMG
  derivatives**의 정상상태 (u̇,v̇,ṙ)=0 3원 연립을 fsolve —
  시간 적분(RK4 수천 스텝)만 생략. 새 물리·새 계수 0 → 원전
  재대조 자체가 불필요 (프록시 오차원 = "과도기 vs 정상" 하나)
- src/physics/maneuvering/proxy.py (steady_turning_state·
  turning_proxy) + builder.py (게이트 구성 사슬 추출 — pipeline
  _maneuvering_gate 30줄 감량 공용화)

## 검증 — 오분류 0

- **존재 증명**: 정상해 = 시뮬 꼬리 (KVLCC2, r·u 5% 이내)
- 보정 TACTICAL_OVER_STEADY 1.15 (선박군 시뮬 대조 1.13~1.20,
  KVLCC2 1.42는 박제 계수·Cb 0.81 별개 모집단 — 정직 각주.
  통통 선형은 프록시 관대 방향 = 오탈락 없음 쪽이라 안전)
- **전수 캘리브레이션**: v2 합격 97척 프록시 D_T 최대 3.12
  (오탈락 0) vs v3 불합격 표본 21척 최소 6.41 (오생존 0) —
  한계 5.0 양쪽 완전 분리, fsolve 수렴 실패 0
- 신규 시험 5 (정상해=꼬리·선박군 정합·실전선 판별·빌더·배선
  사망) — 회귀 **441 전부 통과** (19분 45초)

## 백지 독립 리뷰 첫 실전 (3원칙 §2)

- physics-critic (코드+목표만, 설계 이유 비공유): 지적 6건 —
  **수리 4** (loa=Lpp 관례 명기·xg=0 각주·fsolve u=0 특이점
  가드·잔차 ṙ×Lpp 단위 정합), **근거 기각 2** (자항 이분법
  브래킷 무검증 = 기존 게이트 계보라 백로그·u0 인자 중복 =
  maneuvering_report API 대칭 유지). 원칙 가동 실증

## v4 발사 (배경)

- fast 완전판 (나선+space+roll RMS+**선회 프록시**+구조+EEDI),
  pop 256×gen 200, seed 4 — 진화+전체 재검 한 프로세스
- 기대: v3의 "재검 전멸" 구조 소멸 — fast 전선 ≈ full 전선.
  outputs/nsga_v4.log·pareto_large_v4_evo·v4_full_recheck.csv
- 백로그: solve_self_propulsion 브래킷 상한 검증 (리뷰 이월),
  advance 프록시 (과도기 지배 — 필요 시 Nomoto 1차)

## v4 결산 — 완전 검증 신챔피언 (70/149)

- 진화 생존 34% (5.1만 중 1.7만) · 전선 149척 · **Cb 0.471~0.524
  절충 대역 자기 발견** (v2 통통과 v3 극날씬 사이) · 프록시 D_T
  3.60~5.00 — 진화가 한계선에 정확히 주차 (도망칠 사각지대 소멸)
- **전체 재검 70/149 완전 검증** (v3 0/183에서 구조 반전):
  - 탈락 79척 전원 = **advance 4.50~4.57 vs 4.5 박빙** (프록시
    미판정 항목 — 각주 그대로). 내항 탈락 0
  - **프록시 정밀도 실증**: 경계 후보 시뮬 D_T 4.86~5.01 vs
    프록시 4.90~5.00 — 오차 ~3%, D_T 판정 자체는 정확
- **신챔피언 (완전 검증)**: 경제형 **9.8 kn·Cb 0.479·단가
  0.335 $/kt·nm — v2 대비 −24%**. 속도형(14.6 kn)은 v2 1.657이
  우세 유지 → 공인 전선 = v2 ∪ v4 통합 (속도 축 상보)
- 교훈: 심판을 채우니 진화가 경계에 주차 — fast·full 잔여 격차
  = advance 하나로 수렴 (2단 구도가 그 박빙만 걸러내는 정상
  작동). advance 프록시(Nomoto 1차)는 백로그 (이득 = 재검
  탈락 79척 절약분 뿐이라 저순위)

---

# Streamlit 시연 앱 완주 — 전시장 개장 (33일차분)

- 스펙 §7 훅 대조 후 착수 (전선 데이터 v4 반영) — 플랜
  2026-08-11-streamlit-live-design.md, 2단계 연속 완주
- **구조**: app/ui/* = streamlit 비의존 순수 함수 (시험 6 —
  메쉬→plotly·게이트 3태+꺼짐 매핑·성적표 카드·전선 로더),
  streamlit 렌더는 pages/ 전용 (수동 브라우저 검증)
- **라이브 설계**: 3입력+쌍동 토글 → run_pipeline 임포트 →
  나선 실황 → 판정 배너·8중 신호등·3D 선형·성적표 툴팁.
  실기동: 2m 조사선 ~100초, 내항 불합격 정직 표시
- **갤러리**: v4 완전 검증 70척 탐사기 (vector_json 비노출 —
  라이선스)·설계 지도 12점·연대기 (150→447)·파랑 GIF. 자산
  없으면 정직 안내 ("outputs 로컬 전용")
- 검거 1: 설계 지도 JSON 키 오독 (payload_t → payload_kg) —
  브라우저 실기동이 잡음 (순수 함수 시험 밖 배선 오류)
- 실행: `/opt/anaconda3/bin/python -m streamlit run app/main.py`
  (.claude/launch.json 등록). 공개 배포 절대 없음
- 회귀 445 + 갤러리 2 = **447 전부 통과**

---

# 모델 계층 위임 실전 — 4작업 병렬 (34일차분)

오너 제안 "모델별 작업 배분" → 하위 에이전트 frontmatter `model:`
로 자동 배분 체계 확립 (light-worker haiku·reference-hunter
sonnet·physics-critic opus + CLAUDE.md 백로그×모델 표 규칙화).
첫 실전 4작업 병렬:

- **haiku ×2**: 문서 오탈자 훑기 (1건 수리·4파일 깨끗), 실선
  수집 현황 정리 (**#17 표적 확정 — workboat 배수량형·patrol
  전기추진 표본 부족, 결측 17항목 목록화**)
- **sonnet ×2**: Streamlit 3D 조명·시점 + 전선 추천선 별표
  (브라우저 렌더 검증), 자항 브래킷 가드 TDD
- **자항 브래킷 가드** (백지 리뷰 이월): solve_self_propulsion
  이분법 상한 hi=100rps에서도 net(hi)<0 (저항>최대추력)이면
  조용히 nP≈100 반환하던 것 → 명시 ValueError. net() 단조
  증가라 근 브래킷 밖 판정 정확, 수식 불변. **협업 하이라이트**:
  sonnet이 자기 범위 넘어 "optimize_large 프록시 블록이 이
  ValueError 미포착 — NSGA 죽을 위험, 수정은 메인 몫" 정직
  보고 → 메인이 게이트 사망(오류 아님)으로 분류 (정직 관례
  실전)
- 시험 447 → **448**. 커밋 3건 (오탈자+Streamlit, 브래킷 가드)
- 교훈: 격리 위임 + 정직 관례가 모델 간 협업으로 작동 —
  하위가 경계 넘는 위험을 보고하고 메인이 판단

---

# 실선 수집 회차 — 8후보 → 1척, 정직 검수의 6번째 방어전 (35일차분)

#17 표적(수집 정리로 확정: workboat 배수량형·patrol 전기추진)
수집. sonnet 헌터 웹 사냥 → 메인 정직 검수 (모델 계층 + 검수
관례). **8후보 중 1척만 통과** — 검수가 데이터 오염 7건 차단:

| 후보 | 판정 | 사유 |
|---|---|---|
| **Voltari Patrol 28** | ✅ 채택 (patrol 전기) | 공식 직접 확인·필수필드 완비·물리 통과 (등급 B) |
| Otter | 기각 | 기존 survey 중복 (헌터 오분류) |
| SeaRobotics USV-2600 | 기각 | 소스 간 제원 상충 (41kg/3.0m vs 100kg/3.3m) |
| Eco Marine Aquarius | 기각 | **5m 선체에 폭 8m = 폭 환각 패턴** (O-16 이력 재현) |
| Ocius Bluebottle | 기각 | **만재 650kg·탑재 600kg 물리 모순** + 하이브리드(순수 전기 아님) |
| C-Worker 4 | 기각 | 최대속도(필수) 6/7/9kn 상충 — 확정 불가 |
| SeaRobotics USV-5000 | 기각 | beam·weight_full(필수) 결측 |
| MARTAC MANTAS T12 | 기각 | beam·weight_full(필수) 결측 |

- Voltari: 168kWh 배터리·24° deep-V·22.35 m/s·만재 3175kg —
  patrol 전기추진 표적 정중앙. 검증기 물리 관문(Fn 2.40·L/B
  3.18·B/T 3.42·부양) 전부 통과, 등급 B
- 실선 19척 (patrol 3→4). workboat 표적은 필수필드 클린 후보
  부재로 이월 (억지 추가 안 함 — 정직)
- **검수 방어 실적 6번째**: 폭 환각·중량 모순·소스 상충·필수
  결측을 전부 걸러냄. 헌터가 정직하게 자기 의심을 플래그하고
  (Aquarius beam·USV-2600 상충 자진 표기) 메인이 확정 — 모델
  계층 협업이 데이터 품질로 작동

---

# 다중 에이전트 체계 정비 — 모델 계층·판정층 성문화 (오후 관통)

오너 제안 "다중 에이전트 6대 원칙" → 프로젝트 실정에 맞춰 압축·
성문화. 하루 동안 체계가 층층이 자라 CLAUDE.md에 박힘:

- **3원칙 성문화** (c9f7f46): ① 격리 위임 ② 백지 독립 리뷰
  ③ 무인 배경 완주. 하위 에이전트 2종 신설 (physics-critic·
  reference-hunter) — 이후 physics-critic이 브래킷 버그 검거,
  reference-hunter 계보(sonnet 헌터)가 데이터 8후보 사냥으로 실전
- **경량 온보딩** (0557902): docs/light-work-prompt.md — 타 앱/
  모델용 허용·금지 경계 (물리·게이트·라이선스 방어)
- **모델 계층 자동 배분** (3a0574f·4a42bd8): 에이전트 frontmatter
  `model:` — light-worker(haiku)·reference-hunter(sonnet)·
  physics-critic(opus). 위임 시 모델 자동 교체 (오너 수동 불요).
  **4계층 확정**: Fable(신규 물리 유도)·Opus(표준·critic 독립)·
  Sonnet(사냥·배선)·Haiku(심부름). critic opus 고정 = 독립성 자산
- **백로그×모델 표 규칙화** (b3e6926): "뭐 남았어" 시 추천 모델
  묶어 브리핑. 살아있는 표
- **판정층 규칙** (5eabcb6, 오너 결정): 배경/위임=재료 수집층,
  **채택·해석·기각 판정은 메인 직접**. 판정 무게 큰 회차(신규
  물리·전선/데이터 채택·결산 서사)는 Fable 승급 권고, 동일 결과
  예상 표준 판정은 Opus 유지 (조건부 — 무차 구간 비용 방지)
- 실전 검증 4작업 병렬 (haiku 2·sonnet 2) + 데이터 8→1 검수 —
  하위가 정직 플래그·경계 위험 보고, 메인이 판정. **모델 간에도
  정직 관례가 협업으로 작동** 실증
