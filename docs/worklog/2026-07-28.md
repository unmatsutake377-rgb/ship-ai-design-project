# 작업일지 2026-07-28

> 테스트 135 → 149 · 완료 6건 · 전부 main 병합 · **Phase B 개막**

## 오늘 한 일

1. **Ship-D 선별 파레토 (M5b-2 2차)** — 300척 전수 평가 (통과 134, 파레토 11). 발견: 저속 임무(Fn 0.22)에선 Wigley 저항 우세(7.0 vs 12.1N), Ship-D 중량 우세(126.9 vs 140kg) — **임무 속도가 선형 계열의 승자를 결정**. 트랜섬 박리 미모델 주의 기록.
2. **외부 검토(Gemini) 대응** — 오너가 받아온 검토 채점: 표본 부족 지적 정당 / "GM 0 수학적 껍데기" 과장 (실측 GM 양수, 필터 하한 턱걸이) / 처방(45차원 GA)은 제약 벽 미고려 → 대리모델이 옳은 다음 수임을 논증.
3. **M5b-1 대리모델 + 가상 전수 선별** — torch MPS 2헤드 MLP, 3만 척 예측 → 80척 재검증 → 합산 파레토 15척 (대리모델 발굴 6척). **정직 기록:** 분류 정확도 1.0이 가짜임을 자가 적발 (screen.csv에 feasible만 저장하던 라벨 결손 버그) → 탈락 행 보존으로 수정. 회귀 R² 저조(소표본) 명시 — 재검증 원칙 덕에 결과 수치는 무오염.
4. **ROS2 환경 확정 (오너 결정)** — Mac Docker로 시작, 고부하는 추후 Windows+WSL2 (설정 공유 — 실행 장소 추가 개념). 결정 대기 항목 0이 됨.
5. **Phase B-1** — Docker 검증 → ros:jazzy(arm64 네이티브) pub/echo 왕복 → `ros2_export` (URDF 관성 3축 + 유체계수 YAML, 부호 규약 명시) → **컨테이너 내 check_urdf 통과** ("Successfully Parsed XML" — 우리 산출물이 ROS2 실도구 검증 통과).
6. **#25 1단계 (오너 제안)** — 3입력 UX: `--payload --purpose`만으로 실행, 속도는 용도별 실선 순항 중앙값 자동 (출처 표시, --speed 우선).

## 오너 기여

- 외부 검토 조달 + 검증 요청 (→ 우리 다음 수 논증 강화)
- ROS2 환경 결정 + Docker Desktop 설치
- #25 3입력 UX 제안 (마감 직전 → 당일 1단계 구현됨)

## 내일(다음 세션) 할 일

1. **Phase B-2: Gazebo 스폰** — gz sim 컨테이너 + 우리 배 모델 투입 (SDF 월드, 부력 플러그인). ROS2 세계의 물에 우리 배가 뜨는 순간.
2. B-3: 유체계수 플러그인 연결 + 차동추력 제어 이식 (python_sim의 LOS·게인 재사용)
3. 백로그: #25 2단계(파레토 가중 프리셋), #17 수집, 대리모델 재학습(정상 라벨로)

## 상태 메모

- 결정 대기: 없음 (전부 해소)
- 남은 Phase: B(진행 중) → C(반배수량·활주·CFD — 설계만 존재)
