# Phase A (M3.5) 구현 계획 — 저항 모듈

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 생성된 선형의 전저항(마찰 + 조파)을 계산해 소요 추력·유효 파워를 리포트에 추가.

**Architecture:** `src/physics/resistance.py` 단일 모듈. 마찰저항 = ITTC-57 + 형상계수. 조파저항 = Michell 박선 적분 (일반화 Wigley의 분리형 y=f(x)·h(z) 활용, λ=cosh(u) 치환 수치적분). Holtrop 금지 (spec §2.3 — USV 스케일에서 무효).

**Tech Stack:** numpy (수치적분은 np.trapezoid), 기존 모듈 (hydrostatics의 immersed_mesh로 침수표면적).

**Spec:** 설계 문서 v2 §2.3, §4 (Wigley 벤치마크).

## Global Constraints

- M1~M3 계획의 Global Constraints 전부 승계 (SI, ρ=1025, 한국어 docstring 등).
- 동점성계수 `NU_SEAWATER = 1.19e-6` m²/s (해수 15°C).
- 형상계수 `FORM_FACTOR = 0.10` (세장 선형 개략, 명명 상수 + 리포트 기록).
- Michell 적분 문헌 대비 과대예측 경향 있음 — 벤치마크는 밴드 검증 (자릿수 오류 방지 목적).
- 브랜치: `feat/m3_5-resistance`.

## 파일 구조

```
src/physics/resistance.py    # 신규
tests/test_resistance.py     # 신규
src/pipeline.py              # 수정: 저항·추력·파워 리포트 통합
tests/test_pipeline.py       # 수정: 저항 필드 검증 추가
```

---

### Task 1: ITTC-57 마찰저항

**Files:** Create `src/physics/resistance.py`, `tests/test_resistance.py`

**Interfaces:**
- Produces: `reynolds(speed, length, nu=NU_SEAWATER) -> float`, `ittc_cf(re) -> float`, `frictional_resistance(speed, length, wetted_area, rho=1025.0, form_factor=FORM_FACTOR) -> float`, `wetted_surface(mesh, draft) -> float` (침수부 전체 면적 − 수선면 캡 면적).

- [ ] **Step 1: 실패 테스트** — `tests/test_resistance.py`

```python
import numpy as np
import pytest
import trimesh

from src.physics.resistance import (
    FORM_FACTOR,
    NU_SEAWATER,
    frictional_resistance,
    ittc_cf,
    reynolds,
    wetted_surface,
)


def test_ittc_cf_exact():
    # Re=1e8: Cf = 0.075/(log10(1e8)-2)^2 = 0.075/36
    assert ittc_cf(1e8) == pytest.approx(0.075 / 36.0, rel=1e-12)


def test_reynolds():
    assert reynolds(1.5, 4.0) == pytest.approx(1.5 * 4.0 / NU_SEAWATER, rel=1e-12)


def test_frictional_positive_and_scales():
    """Rf ~ V^1.8~2.0 스케일링 (ITTC 마찰 물리 불변량)."""
    s, L, S = 1.5, 4.0, 3.0
    r1 = frictional_resistance(s, L, S)
    r2 = frictional_resistance(2 * s, L, S)
    assert r1 > 0
    ratio = r2 / r1
    assert 2 ** 1.7 < ratio < 2 ** 2.0


def test_wetted_surface_box():
    """바지선 해석해: S = L·B + 2(L+B)·T (바닥 + 측면, 캡 제외)."""
    L, B, D, t = 4.0, 1.2, 0.6, 0.3
    box = trimesh.creation.box(extents=[L, B, D])
    box.apply_translation([0, 0, D / 2])
    expected = L * B + 2 * (L + B) * t
    assert wetted_surface(box, t) == pytest.approx(expected, rel=1e-3)
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/test_resistance.py -v` → `ModuleNotFoundError`

- [ ] **Step 3: 구현** — `src/physics/resistance.py`

```python
"""저항 추정 (spec §2.3, M3.5).

마찰: ITTC-57 상관선 + 형상계수 (1+k).
조파: Michell 박선이론 적분 (Task 2에서 추가).
Holtrop-Mennen 금지 — 20~300 m 상선 회귀식, USV 스케일 무효 (spec §2.3).
"""
from __future__ import annotations

import math

import numpy as np
import trimesh

from src.physics.hydrostatics import immersed_mesh, waterplane_properties

RHO_SEAWATER = 1025.0
NU_SEAWATER = 1.19e-6   # 해수 15°C 동점성계수 [m²/s]
G = 9.81
FORM_FACTOR = 0.10      # 형상계수 k (세장 선형 개략, 점성압력저항 보정)


def reynolds(speed: float, length: float, nu: float = NU_SEAWATER) -> float:
    return speed * length / nu


def ittc_cf(re: float) -> float:
    """ITTC-57 모형선-실선 상관선: Cf = 0.075 / (log10(Re) - 2)²."""
    return 0.075 / (math.log10(re) - 2.0) ** 2


def wetted_surface(mesh: trimesh.Trimesh, draft: float) -> float:
    """침수 표면적. slice가 만든 수선면 캡(들)은 물에 닿지 않으므로 제외."""
    below = immersed_mesh(mesh, draft)
    aw, _ = waterplane_properties(mesh, draft)
    return float(below.area) - aw


def frictional_resistance(speed: float, length: float, wetted_area: float,
                          rho: float = RHO_SEAWATER,
                          form_factor: float = FORM_FACTOR) -> float:
    """Rf = ½·ρ·V²·S·Cf·(1+k)."""
    cf = ittc_cf(reynolds(speed, length))
    return 0.5 * rho * speed ** 2 * wetted_area * cf * (1.0 + form_factor)
```

- [ ] **Step 4: 통과 확인** — 4 passed
- [ ] **Step 5: 커밋** — `feat: ITTC-57 마찰저항 + 침수표면적`

---

### Task 2: Michell 조파저항

**Files:** Modify `src/physics/resistance.py`, `tests/test_resistance.py`

**Interfaces:**
- Produces: `michell_wave_resistance(loa, beam, draft_design, n, m, draft, speed, rho=1025.0, n_u=120, n_x=160, n_z=80) -> float`
- 수학: Rw = (4ρg²)/(πV²)·∫₁^∞ (P²+Q²)·λ²/√(λ²−1) dλ, P+iQ = ∫∫ (∂y/∂x)·e^(−k₀λ²·d)·e^(ik₀λx) dx dz (d = 수면하 깊이, k₀ = g/V²). λ = cosh(u) 치환으로 특이점 제거. 일반화 Wigley는 y = (B/2)f(x)h(z) 분리형 → x·z 적분 분리로 O(n_u·(n_x+n_z)).

- [ ] **Step 1: 실패 테스트 추가** — `tests/test_resistance.py`에 추가

```python
from src.physics.resistance import michell_wave_resistance

# 표준 Wigley: n=2, m=2 (Cb≈0.444), L/B=10, B/T=1.6 — 문헌 벤치마크 선형
WIGLEY = dict(loa=4.0, beam=0.4, draft_design=0.25, n=2.0, m=2.0, draft=0.25)


def _fn_to_speed(fn, loa=4.0):
    return fn * (9.81 * loa) ** 0.5


def test_michell_positive():
    rw = michell_wave_resistance(**WIGLEY, speed=_fn_to_speed(0.316))
    assert rw > 0


def test_michell_convergence():
    """격자 2배 세분 시 변화 < 2% — 수치적분 수렴 확인."""
    v = _fn_to_speed(0.316)
    r1 = michell_wave_resistance(**WIGLEY, speed=v)
    r2 = michell_wave_resistance(**WIGLEY, speed=v, n_u=240, n_x=320, n_z=160)
    assert abs(r2 - r1) / r2 < 0.02


def test_michell_low_froude_vanishes():
    """조파저항은 저속에서 급감 — Fn 0.1은 Fn 0.316의 1/5 미만."""
    r_low = michell_wave_resistance(**WIGLEY, speed=_fn_to_speed(0.10))
    r_ref = michell_wave_resistance(**WIGLEY, speed=_fn_to_speed(0.316))
    assert r_low < 0.2 * r_ref


def test_michell_magnitude_band():
    """표준 Wigley Fn=0.316: 자릿수 검증 밴드 (문헌 Cw ~1e-3 대역).

    ½ρV²S 기준 Cw로 환산해 [4e-4, 4e-3] 밴드 — 계수/단위 오류(×10)를 잡는
    안전망. 정밀 문헌값 대조는 후속 정제 (Michell은 실험 대비 과대예측 경향).
    """
    v = _fn_to_speed(0.316)
    rw = michell_wave_resistance(**WIGLEY, speed=v)
    s_approx = 4.0 * (0.4 + 2 * 0.25) * 0.75  # 개략 침수면적 [m²]
    cw = rw / (0.5 * 1025.0 * v ** 2 * s_approx)
    assert 4e-4 < cw < 4e-3
```

- [ ] **Step 2: 실패 확인** — ImportError
- [ ] **Step 3: 구현** — `resistance.py`에 추가

```python
def michell_wave_resistance(loa: float, beam: float, draft_design: float,
                            n: float, m: float, draft: float, speed: float,
                            rho: float = RHO_SEAWATER,
                            n_u: int = 120, n_x: int = 160,
                            n_z: int = 80) -> float:
    """Michell 박선이론 조파저항 [N].

    Rw = (4ρg²)/(πV²) ∫₁^∞ (P²+Q²) λ²/√(λ²−1) dλ
    P+iQ = ∫∫ (∂y/∂x)·exp(−k₀λ²·d)·exp(ik₀λx) dx dz  (d: 수면하 깊이)
    λ = cosh(u) 치환: dλ/√(λ²−1) = du → 특이점 제거.
    일반화 Wigley 분리형 y=(B/2)·f(x)·h(z) → x/z 적분 분리.
    draft: 실제(평형) 흘수 — 침수 부분만 적분.
    """
    k0 = G / speed ** 2

    # x 방향: f'(x) = -(2/L)·n·|u|^(n-1)·sign(u), 반폭 계수 B/2 포함
    xs = np.linspace(-loa / 2, loa / 2, n_x)
    ux = 2.0 * xs / loa
    fprime = -(beam / 2.0) * (2.0 / loa) * n * np.abs(ux) ** (n - 1) * np.sign(ux)

    # z 방향: h(z) = 1 − ((T_d−z)/T_d)^m, 수면하 깊이 d = draft − z
    zs = np.linspace(0.0, draft, n_z)
    h = 1.0 - ((draft_design - zs) / draft_design) ** m
    d_below = draft - zs

    us = np.linspace(1e-4, 4.0, n_u)  # λ = cosh(u) ∈ [1, ~27]
    lam = np.cosh(us)

    z_int = np.trapezoid(
        h[None, :] * np.exp(-k0 * lam[:, None] ** 2 * d_below[None, :]),
        zs, axis=1,
    )
    phase = k0 * lam[:, None] * xs[None, :]
    p = np.trapezoid(fprime[None, :] * np.cos(phase), xs, axis=1)
    q = np.trapezoid(fprime[None, :] * np.sin(phase), xs, axis=1)

    integrand = (p ** 2 + q ** 2) * z_int ** 2 * lam ** 2
    return float(4.0 * rho * G ** 2 / (np.pi * speed ** 2)
                 * np.trapezoid(integrand, us))
```

- [ ] **Step 4: 통과 확인** — 벤치마크 밴드 실패 시: (1) 수렴 테스트 먼저 확인 (수치 문제 vs 공식 오류 구분), (2) k₀·λ² 지수의 부호·단위 점검, (3) fprime의 B/2 중복 여부 점검.
- [ ] **Step 5: 커밋** — `feat: Michell 박선 조파저항 (Wigley 벤치마크 밴드 검증)`

---

### Task 3: 전저항 통합 + 파이프라인 연결

**Files:** Modify `src/physics/resistance.py`, `src/pipeline.py`, `tests/test_resistance.py`, `tests/test_pipeline.py`

**Interfaces:**
- Produces: `ResistanceReport(speed, froude, reynolds, wetted_area, cf, form_factor, rf, rw, total, effective_power)` (dataclass), `total_resistance(mesh, dims, n, m, draft, speed) -> ResistanceReport`
- `run_pipeline` 리포트에 `"resistance"` 키 추가 (asdict). 소요 추력 = total [N], 유효 파워 Pe = total·V [W].

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_resistance.py`에:

```python
from src.ai.hull_generator import generate_hull_mesh, solve_exponents
from src.core.types import MainDimensions
from src.physics.resistance import total_resistance


def test_total_resistance_report():
    dims = MainDimensions(loa=4.0, beam=1.3, depth=0.48, draft_design=0.30, cb=0.50)
    mesh = generate_hull_mesh(dims)
    n, m = solve_exponents(dims.cb)
    rep = total_resistance(mesh, dims, n, m, draft=0.20, speed=1.5)
    assert rep.total == pytest.approx(rep.rf + rep.rw, rel=1e-9)
    assert rep.effective_power == pytest.approx(rep.total * 1.5, rel=1e-9)
    assert rep.rf > 0 and rep.rw > 0
    assert rep.wetted_area > 0
```

`tests/test_pipeline.py`의 `test_run_pipeline_survey` 필수 필드에 `"resistance"` 추가:

```python
    for key in ("goal", "dimensions", "regime", "weights", "hydrostatics",
                "resistance", "passed", "mesh_file"):
        assert key in report, key
    assert report["resistance"]["total"] > 0
```

- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 구현**

`resistance.py`에:

```python
from dataclasses import dataclass

from src.core.types import MainDimensions


@dataclass(frozen=True)
class ResistanceReport:
    speed: float
    froude: float
    reynolds: float
    wetted_area: float
    cf: float
    form_factor: float
    rf: float               # 마찰저항 [N]
    rw: float               # 조파저항 [N]
    total: float            # 전저항 = 소요 추력 [N]
    effective_power: float  # Pe = R·V [W]


def total_resistance(mesh: trimesh.Trimesh, dims: MainDimensions,
                     n: float, m: float, draft: float,
                     speed: float, rho: float = RHO_SEAWATER) -> ResistanceReport:
    s_wet = wetted_surface(mesh, draft)
    re = reynolds(speed, dims.loa)
    cf = ittc_cf(re)
    rf = frictional_resistance(speed, dims.loa, s_wet, rho)
    rw = michell_wave_resistance(dims.loa, dims.beam, dims.draft_design,
                                 n, m, draft, speed, rho)
    total = rf + rw
    return ResistanceReport(
        speed=speed, froude=speed / math.sqrt(G * dims.loa), reynolds=re,
        wetted_area=s_wet, cf=cf, form_factor=FORM_FACTOR,
        rf=rf, rw=rw, total=total, effective_power=total * speed,
    )
```

`pipeline.py` 수정 — import에 `from src.ai.hull_generator import solve_exponents`, `from src.physics.resistance import total_resistance` 추가, `run_pipeline`에서 hydro 계산 뒤:

```python
    n_exp, m_exp = solve_exponents(dims.cb)
    resist = total_resistance(mesh, dims, n_exp, m_exp,
                              draft=hydro.draft, speed=goal.target_speed_ms)
```

report dict에 `"resistance": dataclasses.asdict(resist),` 추가. `_print_summary`에:

```python
    r = report["resistance"]
    print(f"저항 @목표속도  : 전체 {r['total']:.1f} N "
          f"(마찰 {r['rf']:.1f} + 조파 {r['rw']:.1f})")
    print(f"유효 파워       : {r['effective_power']:.1f} W")
```

- [ ] **Step 4: 전체 스위트 + 실물 실행** — `python -m pytest -q` 전부 통과, `python -m src.pipeline --speed 1.5 --payload 100 --purpose survey --out outputs/demo` 저항 줄 출력 확인
- [ ] **Step 5: 커밋 + 푸시** — `feat: M3.5 전저항 통합 (소요 추력·유효 파워 리포트)`

---

## Self-Review

1. **커버리지:** spec §2.3 저항(ITTC-57+Michell) ✓, Holtrop 금지 ✓, §4 Wigley 벤치마크(밴드+수렴) ✓, 소요 추력·파워 ✓. 정밀 문헌값 대조는 밴드로 대체 — 한계를 테스트 docstring에 명시.
2. **플레이스홀더:** 없음.
3. **타입 일관성:** `solve_exponents` 반환 (n,m) = michell 인자 ✓, `hydro.draft` = `total_resistance(draft=)` ✓, numpy 2.x `np.trapezoid` 사용 (1.x `trapz` 별칭 아님 — anaconda numpy 2.x 확인됨).
