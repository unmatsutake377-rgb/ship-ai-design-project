# 선박 AI 설계 프로젝트 — 에이전트 운용 원칙

학습 프로젝트 상시 계약(학부 2학년 눈높이 설명·회차 마감 4축·
superpowers 워크플로·main 직커밋)은 메모리·worklog가 정본.
이 문서는 **다중 에이전트 운용 3원칙**을 성문화한다 (원안:
오너의 다중 에이전트 아키텍처 6대 원칙 — 프로젝트 실정에 맞게
2026-08-10 합의로 압축).

## 1. 격리 위임 (Micro-Agent, 컨텍스트 오염 방지)

- 탐색·검색·기계적 수정(오탈자·단순 치환·파일 위치 찾기)은
  메인 스레드가 직접 하지 말고 **격리 하위 에이전트에 위임**,
  요약 결과만 회수한다 (사고 과정·대량 로그를 메인 컨텍스트에
  붓지 않기).
- 단, **물리 구현·원전 대조·회차 서사는 메인 스레드 몫** —
  한 컨텍스트가 원전·관례·실패 박물관을 연속으로 기억하는 것이
  이 프로젝트의 품질 원천이라 조각내지 않는다.
- 하위 에이전트에는 작업에 필요한 최소 도구·최소 컨텍스트만.

## 2. 백지 독립 리뷰 (Need-to-Know Critic)

- 회차의 핵심 코드는 마감 전 **백지 컨텍스트 리뷰 1회**:
  리뷰어에게는 "검토할 코드 조각 + 해결 목표"만 주고, 메인
  스레드의 사고 과정·설계 이유는 공유하지 않는다 (집단 사고
  차단 — 내 가정이 리뷰까지 통과하는 것 방지).
- 물리 코드는 `physics-critic` 에이전트 (수식·단위·부호 관례
  전용), 일반 diff는 `caveman:cavecrew-reviewer`.
- 리뷰 지적은 "정직 관례"로 처리: 반박 가능하면 근거 명시 후
  기각 가능, 맞으면 즉시 수리 — 무시 금지.

## 3. 무인 배경 완주 (Headless Batch)

- 10분 넘는 계산(NSGA·대량 재판정·라벨 적립)은 대화 스레드에서
  대기하지 않는다: **nohup 독립 프로세스 + PYTHONPATH="$PWD" +
  로그 파일 + 감시(Monitor) 통지** 관례로 배경 완주.
- 배경 스크립트는 중간 확인 없이 끝까지 실행되도록 작성 (진행
  이정표를 stdout에 flush — EVOLVE_DONE 류 완료 신호 필수).
- 자동 승인 전면화(-y)는 하지 않는다: 가역 작업은 자동 진행,
  파괴 작업(삭제·강제 푸시·덮어쓰기)만 확인.

## 고정 제약 (요약)

- Ship-D 재배포·생성 선체 STL/렌더 커밋·공개 금지, 전선 CSV는
  outputs/ 로컬만 (references/·outputs/ gitignore).
- 시험 전체 통과 없이 회차 마감 없음. 정직 스킵 관례
  ("데이터 없음 ≠ 불합격") 유지.
